infra_redesign_auto/
├── main.tf                # Root configuration file: sets up providers and calls modules.
├── variables.tf           # Root file declaring global variables used across the project.
├── outputs.tf             # Root file exposing outputs from modules for external use.
├── terraform.auto.tfvars  # Auto-loaded variables file to override defaults or provide environment-specific values.
└── modules/
    ├── core-services/
    │   ├── vpc/
    │   │   ├── main.tf       # Defines VPC, subnets, and IGW.
    │   │   ├── variables.tf  # Declarations for VPC-specific variables.
    │   │   └── outputs.tf    # Exposes VPC-related outputs (e.g., VPC ID, subnet IDs).
    │   ├── rds/
    │   │   ├── main.tf       # Defines RDS resources (DB instance & subnet group).
    │   │   ├── variables.tf  # Declarations for RDS-specific variables.
    │   │   └── outputs.tf    # Exposes RDS outputs such as the DB endpoint.
    │   ├── redis/
    │   │   ├── main.tf       # Defines Elasticache Redis replication group.
    │   │   ├── variables.tf  # Declarations for Redis-specific variables.
    │   │   └── outputs.tf    # Exposes Redis outputs such as the replication group ID.
    │   └── monitoring/
    │       ├── main.tf       # Creates CloudWatch alarms for monitoring resources.
    │       ├── variables.tf  # Declarations for monitoring-specific variables.
    │       └── outputs.tf    # Exposes monitoring outputs (e.g., alarm name).
    └── environments/
        ├── staging/
        │   └── ec2/
        │       ├── main.tf       # Defines EC2 instance configuration for the staging environment.
        │       ├── variables.tf  # Declarations for staging EC2-specific variables.
        │       └── outputs.tf    # Exposes staging EC2 outputs such as instance ID and public IP.
        └── production/
            └── ec2/
                ├── main.tf       # Defines EC2 instance configuration for the production environment.
                ├── variables.tf  # Declarations for production EC2-specific variables.
                └── outputs.tf    # Exposes production EC2 outputs such as instance ID and public IP.
